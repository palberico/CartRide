// Re-exported for backward compatibility — use src/constants/cities.js for new code
import { CITIES } from './cities';
export const DAYBREAK_CENTER = CITIES.daybreak.center;
export const DAYBREAK_BOUNDARY = CITIES.daybreak.boundary;
